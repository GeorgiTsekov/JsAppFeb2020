{{> header}}

<div class="profile col-md-6 text-center col-lg">
    <img class="profile-img" src="./images/user.png" />
    <div class="profile-info">
        <p>Username: <small>{{email}}</small></p> 
        <p class="infoType">Wished {{wishedposts}} posts =)</p>
        {{#each posts}}
        <p>{{this}}</p>
        {{else}}
        <p>No posts</p>
        {{/each}}
    </div>
</div>

{{> footer}}